package api;

public class isNumber {
    private boolean isNumber;

    public isNumber(boolean isNumber) {
        this.isNumber = isNumber;
    }

    public isNumber() {

    }

    public boolean getIsNumber() {
        return isNumber;
    }

    public void setIsNumber(boolean isNumber) {
        this.isNumber = isNumber;
    }
}
